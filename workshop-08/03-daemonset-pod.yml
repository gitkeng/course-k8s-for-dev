apiVersion: apps/v1
kind: DaemonSet
metadata:
  namespace: workshop-08
  name: my-daemonset
spec:
  selector:
    matchLabels:
      app: my-daemonset
  template:
    metadata:
      labels:
        app: my-daemonset
    spec:
      containers:
        - name: nginx
          image: nginx:latest
